{
    "$schema": "vendor/infection/infection/resources/schema.json",
    "source": { "directories": ["src"] },
    "logs": {
        "text": "infection.log",
        "summary": "infection-summary.log"
    },
    // Strict defaults; lower these only for legacy projects with an explicit ratchet plan.
    "minMsi": 90,
    "minCoveredMsi": 95,
    "threads": "max",
    "testFramework": "phpunit",
    "tmpDir": ".infection"
}
