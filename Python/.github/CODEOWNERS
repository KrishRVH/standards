# The enforcement surface. Replace @OWNER with a real human owner. The leading
# catch-all is deliberate: source-level mutation classifications and diagnostic
# suppressions can live anywhere, so every change needs the named owner's review.
# Enable the complete protected-branch checklist in README.md; CODEOWNERS alone
# does not dismiss stale approvals or prevent bypass. pyproject.toml, ruff.toml, and
# pyrightconfig.json are unanchored on purpose: Ruff resolves the nearest
# config and pyrightconfig.json outranks pyproject.toml, so a nested or root
# copy shadows the wall; .basedpyright/ holds baselines that silence
# pre-existing errors. If the mise task fragment (20-python.toml) lives
# outside /.config/mise/, add its path here.
* @OWNER
.basedpyright/ @OWNER
.ruff.toml @OWNER
/.config/mise/ @OWNER
/.github/ @OWNER
/.mutmut-floor @OWNER
/AGENTS.md @OWNER
pyproject.toml @OWNER
pyrightconfig.json @OWNER
ruff.toml @OWNER
