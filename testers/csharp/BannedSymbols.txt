// Banned-API wall (Microsoft.CodeAnalysis.BannedApiAnalyzers, fires as RS0030).
// Each entry mechanizes a rule from AGENTS.md; the message names the
// replacement, not just the ban. Escape per site with
// [SuppressMessage("ApiDesign", "RS0030", Justification = "...")] naming the
// invariant; SA1404 rejects a missing, blank, or <Pending> justification.
T:System.Threading.Mutex;Cross-process locking is shared mutable state; give the state one owner and send it messages, or justify the OS handle per site
T:System.Threading.ReaderWriterLock;Reader-writer locking hides ownership; give the state one owner and send it messages
T:System.Threading.ReaderWriterLockSlim;Reader-writer locking hides ownership; give the state one owner and send it messages
T:System.ThreadStaticAttribute;Thread-affine ambient state; put the value on the owning component and pass it explicitly
T:System.Threading.AsyncLocal`1;Ambient flow-through state; pass context explicitly through parameters
P:System.DateTime.Now;Ambient wall-clock time; inject TimeProvider and use GetLocalNow()
P:System.DateTime.UtcNow;Ambient wall-clock time; inject TimeProvider and use GetUtcNow()
P:System.DateTimeOffset.Now;Ambient wall-clock time; inject TimeProvider and use GetLocalNow()
P:System.DateTimeOffset.UtcNow;Ambient wall-clock time; inject TimeProvider and use GetUtcNow()
M:System.Threading.Thread.Sleep(System.Int32);Blocking delay defeats cancellation; use Task.Delay with a CancellationToken under an async owner
M:System.Threading.Thread.Sleep(System.TimeSpan);Blocking delay defeats cancellation; use Task.Delay with a CancellationToken under an async owner
M:System.Environment.Exit(System.Int32);Skips finalization and hides the exit path; return an exit code from the entry point
