version: "2"

run:
  timeout: 3m
  tests: true

linters:
  default: none
  enable:
    - bidichk
    - bodyclose
    - copyloopvar
    - depguard
    - dupl
    - durationcheck
    - errcheck
    - errchkjson
    - errorlint
    - forbidigo
    - gocheckcompilerdirectives
    - gocognit
    - goconst
    - gocritic
    - gosec
    - ineffassign
    - misspell
    - nilerr
    - nilnesserr
    - nilnil
    - noctx
    - nolintlint
    - perfsprint
    - predeclared
    - recvcheck
    - revive
    - staticcheck
    - thelper
    - unconvert
    - unparam
    - unused
    - usetesting
    - wastedassign
  settings:
    depguard:
      rules:
        no-lazy-iterators:
          deny:
            - pkg: "iter$"
              desc: >-
                iter.Seq, iter.Seq2, and iter.Pull are forbidden; materialize
                dependency-returned iterators at the call boundary
    dupl:
      threshold: 120
    forbidigo:
      analyze-types: true
      exclude-godoc-examples: false
      forbid:
        - pattern: '^maps\.(All|Keys|Values)$'
          pkg: '^maps$'
          msg: banned iterator producer; use a plain map loop
        - pattern: '^slices\.(All|Backward|Chunk|Values)$'
          pkg: '^slices$'
          msg: banned iterator producer; use a plain indexed or range loop
        - pattern: '^strings\.(FieldsFuncSeq|FieldsSeq|Lines|SplitAfterSeq|SplitSeq)$'
          pkg: '^strings$'
          msg: banned iterator producer; use eager strings helpers or bufio.Scanner
        - pattern: '^bytes\.(FieldsFuncSeq|FieldsSeq|Lines|SplitAfterSeq|SplitSeq)$'
          pkg: '^bytes$'
          msg: banned iterator producer; use eager bytes helpers or bufio.Scanner
    gocognit:
      min-complexity: 15
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
      disabled-checks:
        - commentedOutCode
        - hugeParam
    nolintlint:
      require-explanation: true
      require-specific: true
    revive:
      severity: error
      rules:
        - name: blank-imports
        - name: context-as-argument
        - name: early-return
        - name: error-naming
        - name: error-return
        - name: error-strings
        - name: exported
        - name: increment-decrement
        - name: range
        - name: receiver-naming
        - name: time-naming
        - name: var-declaration
        - name: var-naming

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
