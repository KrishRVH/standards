[settings]
lockfile = true

[tasks.check]
description = "Run the Elixir tester gate"
depends = ["check:local"]

[tasks."check:local"]
description = "Run the Elixir tester gate"
depends = ["elixir:check"]
