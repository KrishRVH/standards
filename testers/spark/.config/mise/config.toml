[settings]
lockfile = true

[tasks.check]
description = "Run the SPARK/Ada tester gate"
depends = ["check:local"]

[tasks."check:local"]
description = "Run the SPARK/Ada tester gate"
depends = ["spark:check"]
