[settings]
lockfile = true

[tasks.check]
description = "Run the Fortran tester gate"
depends = ["check:local"]

[tasks."check:local"]
description = "Run the Fortran tester gate"
depends = ["fortran:check"]
