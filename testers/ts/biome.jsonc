{
  "$schema": "https://biomejs.dev/schemas/2.5.5/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true,
  },

  "files": {
    "ignoreUnknown": true,
  },

  "formatter": {
    "enabled": true,
    "formatWithErrors": false,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineEnding": "lf",
    "lineWidth": 120,
  },

  "javascript": {
    "formatter": {
      "quoteStyle": "single",
      "semicolons": "always",
      "trailingCommas": "all",
    },
  },

  "assist": {
    "enabled": true,
    "actions": {
      "preset": "recommended",
      "source": {
        // Match the primary ESLint sort-imports rule so the two supported
        // profiles never propose opposite repairs.
        "organizeImports": {
          "level": "on",
          "options": {
            "identifierOrder": "lexicographic",
          },
        },
      },
    },
  },

  "linter": {
    "enabled": true,
    "domains": {
      "project": "recommended",
      "test": "recommended",
      "types": "recommended",
    },
    "rules": {
      "preset": "all",
      "correctness": {
        "noNodejsModules": "off",
        "noUnresolvedImports": "off",
        "useImportExtensions": "off",
      },
      "performance": {
        "useTopLevelRegex": "off",
      },
      "style": {
        // A compact tagged-error algebra is clearer together than split across
        // one file per class, and public TypeScript modules export in place.
        "noExcessiveClassesPerFile": "off",
        "noExcessiveLinesPerFile": "off",
        "useExportsLast": "off",
      },
    },
  },

  "overrides": [
    // Keep this alternative config compatible with Option A's Prettier gate.
    {
      "includes": ["biome.jsonc"],
      "json": {
        "formatter": {
          "trailingCommas": "all",
        },
      },
    },
    {
      "includes": ["**/*.d.ts"],
      "linter": {
        "rules": {
          "style": {
            "noNamespace": "off",
          },
        },
      },
    },
    {
      "includes": ["**/*.config.{js,mjs,ts}"],
      "linter": {
        "rules": {
          "style": {
            "noDefaultExport": "off",
          },
        },
      },
    },
    {
      "includes": ["**/*.{test,spec}.{js,jsx,ts,tsx}", "**/__tests__/**/*.{js,jsx,ts,tsx}"],
      "linter": {
        "rules": {
          "style": {
            "noMagicNumbers": "off",
          },
        },
      },
    },
  ],
}
