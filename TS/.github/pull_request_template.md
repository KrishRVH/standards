### What does this PR do?

<!-- This body becomes the permanent squash-commit message. Keep it true. -->

### How did you verify your code works?

<!--
The handoff report, per AGENTS.md. Name the test that fails without this
change (or say why none can exist). List every gate you ran and every one
you skipped, with why. Carry forward verbatim: new eslint-disable and
@ts-expect-error sites with their reasons, surviving mutants and the
current Stryker score against the break ratchet, every comment-ignored mutant
with its source reason, the full report's freshly tested-mutant count (or the
incremental report's tested-or-compatibly-reused count), and knip or audit
findings. State only what a command proved; never overstate.
-->
